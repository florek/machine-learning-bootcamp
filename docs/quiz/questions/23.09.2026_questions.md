# Quiz ML Bootcamp – 23.09.2026

Skład: **10 pytań z ostatniej lekcji** (klasyfikacyjne drzewo decyzyjne) + **10 z wcześniejszych lekcji**.

---

## Pytanie 1 [łatwe] [ML]

Metoda `score()` wytrenowanego `DecisionTreeClassifier` zwraca:

A) współczynnik determinacji R²  
B) średni błąd absolutny (MAE)  
C) accuracy (odsetek poprawnych etykiet)  
D) entropię Shannona liści

---

## Pytanie 2 [łatwe] [ML]

Przy `DecisionTreeClassifier(max_depth=1)` drzewo:

A) wykonuje co najwyżej jeden poziom podziału (jeden próg na jednej cesze)  
B) zawsze używa wszystkich czterech cech Iris jednocześnie  
C) nie wykonuje żadnego podziału i przewiduje losową klasę  
D) buduje dokładnie 15 liści niezależnie od danych

---

## Pytanie 3 [łatwe] [ML]

W liściu `DecisionTreeClassifier` model przewiduje:

A) średnią wartość ciągłego targetu w regionie  
B) medianę odległości do sąsiadów  
C) współczynnik R² dla tego regionu  
D) klasę większościową wśród obserwacji w tym liściu

---

## Pytanie 4 [średnie] [ML]

Funkcja `plot_decision_regions` służy przede wszystkim do:

A) obliczania zysku informacyjnego dla każdego możliwego progu  
B) rysowania kolorowych regionów klas i punktów w przestrzeni cech  
C) eksportu drzewa do formatu DOT  
D) skalowania cech przed `fit()`

---

## Pytanie 5 [łatwe] [ML]

`export_graphviz` z nazwami cech i klas służy do:

A) wygenerowania opisu struktury drzewa (węzły, progi, klasy) do wizualizacji  
B) automatycznego podziału danych na train i test  
C) obliczania accuracy na zbiorze testowym  
D) zamiany cech kategorycznych na dummy variables

---

## Pytanie 6 [średnie] [ML]

W przestrzeni dwóch cech granice `DecisionTreeClassifier` mają typowo kształt:

A) gładkiej linii prostej jak w regresji logistycznej  
B) nieregularnych, „poszarpanych” regionów jak przy KNN z k=1  
C) regionów prostokątnych / schodkowych (podziały prostopadłe do osi)  
D) okręgów wokół centroidów klas

---

## Pytanie 7 [średnie] [ML]

Zwiększanie `max_depth` drzewa klasyfikacyjnego przy ocenie wyłącznie na danych treningowych zwykle:

A) zawsze obniża accuracy na train  
B) zwiększa złożoność regionów i często podnosi accuracy na train  
C) wymusza użycie kryterium `'entropy'` zamiast `'gini'`  
D) usuwa potrzebę ustawiania `random_state`

---

## Pytanie 8 [trudne] [ML]

Dlaczego samo `classifier.score(X, y)` na tych samych danych, na których trenowano głębokie drzewo, jest niewystarczające?

A) bo wysoka accuracy na train może ukrywać overfitting – brakuje oceny na zbiorze testowym  
B) bo `score()` klasyfikatora zawsze zwraca R², nie accuracy  
C) bo drzewo nie ma metody `score()`  
D) bo `max_depth` nie wpływa na dopasowanie do train

---

## Pytanie 9 [trudne] [ML]

Redukcja Iris do pary cech przed trenowaniem drzewa pod wizualizację granic oznacza, że:

A) model automatycznie odzyskuje pozostałe cechy przy `predict()`  
B) accuracy na dwóch cechach jest zawsze identyczna jak na czterech  
C) `max_depth` przestaje mieć znaczenie  
D) tracimy informację z odrzuconych atrybutów – obraz jest uproszczony

---

## Pytanie 10 [trudne] [ML]

Które zestawienie kształtów granic w 2D jest poprawne?

A) drzewo → linia prosta; KNN → prostokąty; regresja logistyczna → okręgi  
B) drzewo → okręgi; KNN → linia prosta; regresja logistyczna → schodki  
C) drzewo i KNN → zawsze ta sama linia prosta; regresja logistyczna → schodki  
D) drzewo → regiony prostopadłe do osi; KNN → nieregularne (zależne od k); regresja logistyczna → granica liniowa

---

## Pytanie 11 [łatwe] [ML]

Dla klasyfikacji binarnej z rozkładem klas 50/50 wskaźnik Gini osiąga maksimum równe:

A) 1,0  
B) 0,5  
C) 0,667  
D) 0,0

---

## Pytanie 12 [średnie] [ML]

Zysk informacyjny (IG) przy podziale węzła to:

A) nieczystość rodzica minus ważona średnia nieczystości dzieci  
B) suma Gini wszystkich liści bez wag  
C) różnica accuracy train − accuracy test  
D) iloczyn `max_depth` i liczby cech

---

## Pytanie 13 [średnie] [ML]

Poprawne użycie `StandardScaler` w pipeline klasyfikacji to:

A) `fit` na train i test osobno (inne μ i σ dla każdego zbioru)  
B) `fit` tylko na test, `transform` na train  
C) `fit` tylko na train, `transform` na train i test  
D) `fit` i `transform` wyłącznie na całym zbiorze po połączeniu train+test

---

## Pytanie 14 [trudne] [ML]

Wywołanie `scipy.stats.entropy([...])` bez `base=2`:

A) zawsze zwraca wynik w bitach Shannona  
B) używa logarytmu naturalnego – inna skala niż entropia z log₂  
C) jest równoważne `criterion='gini'`  
D) wymusza `max_depth=1` w drzewie

---

## Pytanie 15 [średnie] [ML]

W porównaniu z MAE, metryka MSE:

A) ignoruje znaki błędów poprzez wartość bezwzględną, bez kwadratów  
B) zawsze zwraca wynik w przedziale [0, 1]  
C) jest tożsama z accuracy klasyfikatora  
D) kwadratuje błędy, więc silniej karze duże odchylenia

---

## Pytanie 16 [trudne] [ML]

Wartość R² < 0 oznacza, że:

A) model idealnie przewiduje każdą obserwację  
B) model jest gorszy niż przewidywanie średnią wartością targetu  
C) dane nie mają żadnej wariancji  
D) konieczne jest użycie `DecisionTreeClassifier` zamiast regresji

---

## Pytanie 17 [średnie] [ML]

Regresja wielomianowa w typowym workflow scikit-learn polega na:

A) rozszerzeniu cech o potęgi (`PolynomialFeatures`) i dopasowaniu `LinearRegression`  
B) zastąpieniu `LinearRegression` przez `DecisionTreeClassifier`  
C) użyciu wyłącznie `StandardScaler` bez modelu  
D) ustawieniu `max_depth` w `LinearRegression`

---

## Pytanie 18 [trudne] [ML]

Uzupełnianie braków metodą `bfill` w kontekście prognozowania szeregów czasowych jest ryzykowne, bo:

A) zawsze usuwa wszystkie wiersze z brakami  
B) działa wyłącznie na cechach kategorycznych  
C) korzysta z przyszłych wartości, co może wprowadzić data leakage  
D) wymusza użycie `drop_first=True`

---

## Pytanie 19 [łatwe] [ML]

KNN podczas `fit()` przede wszystkim:

A) zapamiętuje dane treningowe (lazy learning) – klasyfikacja następuje przy `predict()`  
B) buduje głębokie drzewo decyzyjne z podziałami osiowymi  
C) minimalizuje binary cross-entropy metodą gradientu  
D) dopasowuje prostą linię w przestrzeni cech

---

## Pytanie 20 [średnie] [ML]

W backward elimination w każdej iteracji usuwa się zmienną:

A) o najniższym współczynniku R²  
B) o najwyższym p-value (najmniej istotną przy ustalonym progu, np. 0,05)  
C) o najwyższej wartości `max_depth`  
D) wylosowaną ze zbioru testowego
