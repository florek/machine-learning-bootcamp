# Quiz ML Bootcamp – 09.07.2026

---

## Pytanie 1 [łatwe] [ML]

Metoda `score()` estymatora `LinearRegression` zwraca:

A) średni błąd bezwzględny (MAE)  
B) dokładność klasyfikacji (accuracy)  
C) współczynnik determinacji R²  
D) pierwiastek z błędu średniokwadratowego (RMSE)

---

## Pytanie 2 [łatwe] [ML]

Średni błąd bezwzględny (MAE) ma jednostki:

A) takie same jak zmienna docelowa (target)  
B) zawsze wyrażone w procentach  
C) zawsze bez jednostek (wartość bezwymiarowa)  
D) kwadrat jednostek zmiennej docelowej

---

## Pytanie 3 [łatwe] [Python]

`SimpleImputer(strategy="constant", fill_value=99.0)` stosuje się gdy:

A) chcemy zastąpić braki stałą wartością zastępczą (np. jawny placeholder)  
B) chcemy automatycznie usunąć wiersze z brakami  
C) chcemy uzupełnić braki medianą kolumny  
D) chcemy interpolować braki na podstawie sąsiednich wierszy

---

## Pytanie 4 [łatwe] [ML]

Krzywa predykcji `DecisionTreeRegressor` na wykresie cecha vs target ma typowo postać:

A) idealnej prostej regresji liniowej  
B) gładkiej krzywej sigmoidalnej  
C) pojedynczego poziomego poziomu na całym zakresie  
D) „schodków” – stałych wartości na przedziałach osi cechy

---

## Pytanie 5 [łatwe] [ML]

W modelu OLS (statsmodels) p-value zmiennej mniejsze niż 0,05 oznacza, że zmienna jest:

A) zawsze najsilniejszym predyktorem w zbiorze  
B) nieistotna statystycznie i powinna zostać usunięta  
C) istotna statystycznie  
D) automatycznie skorelowana z interceptem

---

## Pytanie 6 [łatwe] [Python]

Ustawienie `np.random.seed(42)` przed generowaniem danych lub podziałem train/test służy do:

A) zapewnienia powtarzalności wyników przy tym samym ziarnie  
B) zwiększenia poziomu szumu w danych syntetycznych  
C) automatycznego skalowania cech StandardScalerem  
D) wymuszenia stratifikacji klas w train_test_split

---

## Pytanie 7 [średnie] [ML]

W porównaniu z MAE, metryka MSE (Mean Squared Error):

A) ma te same jednostki co target bez transformacji  
B) silniej karze duże błędy dzięki kwadratowi różnicy  
C) ignoruje błędy większe niż średnia  
D) zawsze przyjmuje wartości ujemne

---

## Pytanie 8 [średnie] [ML]

W ręcznej implementacji gradient descent aktualizacja wag ma postać:

A) weights = weights + eta * gradient  
B) weights = gradient / eta  
C) weights = eta * X.T  
D) weights = weights − eta * gradient

---

## Pytanie 9 [średnie] [ML]

Typowy objaw underfittingu (niedouczenia) to sytuacja, gdy:

A) score na train jest wyraźnie wyższy niż na test  
B) model osiąga R² = 1,0 na train i 0,5 na test  
C) score na train i test są podobne i niskie  
D) macierz pomyłek ma same zera poza diagonalą

---

## Pytanie 10 [średnie] [ML]

F1-score dla danej klasy jest:

A) średnią harmoniczną precision i recall  
B) sumą arytmetyczną precision i recall  
C) iloczynem precision i recall  
D) różnicą między recall a precision

---

## Pytanie 11 [średnie] [ML]

Zwiększanie `max_depth` w `DecisionTreeRegressor` bez innych ograniczeń:

A) zawsze poprawia generalizację na zbiorze testowym  
B) zmniejsza liczbę liści i upraszcza model  
C) zwiększa złożoność modelu i ryzyko przeuczenia  
D) eliminuje potrzebę podziału train/test

---

## Pytanie 12 [średnie] [ML]

Metryka `max_error` w ocenie regresji mierzy:

A) średnią z kwadratów błędów predykcji  
B) największą wartość bezwzględną błędu |y_true − y_pred|  
C) procent wariancji wyjaśnionej przez model (R²)  
D) pierwiastek z błędu średniokwadratowego

---

## Pytanie 13 [średnie] [ML]

Metoda `predict_proba()` w `LogisticRegression` zwraca:

A) wyłącznie etykiety klas 0 lub 1  
B) współczynniki regresji dla każdej cechy  
C) prawdopodobieństwa przynależności do każdej klasy  
D) odległości euklidesowe do k najbliższych sąsiadów

---

## Pytanie 14 [średnie] [ML]

Granice decyzyjne drzewa klasyfikacyjnego w przestrzeni cech mają charakter:

A) podziałów prostopadłych do osi (kształt schodkowy)  
B) zawsze idealnie okrągłych jak w KNN z małym k  
C) pojedynczej linii prostej jak w regresji logistycznej  
D) losowych – zależnych wyłącznie od ziarna random_state

---

## Pytanie 15 [trudne] [ML]

W automatycznej backward elimination (statsmodels) w każdej iteracji usuwa się zmienną o:

A) najniższym współczynniku regresji  
B) najwyższym p-value (najmniej istotną statystycznie)  
C) najwyższej korelacji z targetem  
D) najmniejszej liczbie obserwacji niezerowych

---

## Pytanie 16 [trudne] [ML]

KNN jest klasyfikowany jako algorytm „leniwy” (lazy learning), ponieważ podczas `fit()`:

A) minimalizuje funkcję kosztu binary cross-entropy  
B) buduje rekurencyjnie strukturę drzewa decyzyjnego  
C) uczy współczynników regresji liniowej metodą najmniejszych kwadratów  
D) głównie zapamiętuje dane treningowe, a klasyfikacja następuje przy `predict()`

---

## Pytanie 17 [trudne] [ML]

Ujemny współczynnik R² (r2_score) dla modelu regresji oznacza, że model:

A) jest gorszy niż przewidywanie średnią wartością targetu  
B) idealnie dopasowuje dane treningowe  
C) ma zbyt mało cech i wymaga PolynomialFeatures  
D) został wytrenowany wyłącznie na zbiorze testowym

---

## Pytanie 18 [trudne] [Python]

Metoda `bfill` (backward fill) przy uzupełnianiu braków w szeregu czasowym używanym do prognozowania:

A) może wprowadzić data leakage, bo korzysta z przyszłych obserwacji  
B) jest zawsze bezpieczniejsza niż forward fill  
C) uzupełnia braki wyłącznie wartościami z wcześniejszych obserwacji  
D) działa identycznie jak interpolacja liniowa

---

## Pytanie 19 [trudne] [ML]

W porównaniu z KNN, drzewo decyzyjne podczas `fit()`:

A) jedynie zapamiętuje surowe dane bez budowy struktury  
B) uczy struktury podziałów (parametryczne w sensie struktury drzewa)  
C) wymaga wcześniejszego skalowania cech StandardScalerem  
D) nie obsługuje klasyfikacji wieloklasowej

---

## Pytanie 20 [trudne] [ML]

Entropia Shannona (log₂) dla rozkładu [0,8, 0,2] w porównaniu z [0,5, 0,5]:

A) jest wyższa, bo dominuje jedna klasa  
B) jest identyczna dla każdego rozkładu binarnego  
C) wynosi dokładnie 1 bit w obu przypadkach  
D) jest niższa, bo rozkład jest bardziej nierównomierny (mniej niepewności)
