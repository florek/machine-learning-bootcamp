# Quiz ML Bootcamp – 24.09.2026

Skład: **10 pytań z ostatniej lekcji** (klasyfikacyjne drzewo decyzyjne) + **10 z wcześniejszych lekcji**.

---

## Pytanie 1 [łatwe] [ML]

Domyślne kryterium podziału w `DecisionTreeClassifier` to:

A) `'entropy'`  
B) `'gini'`  
C) `'mse'`  
D) `'mae'`

---

## Pytanie 2 [łatwe] [ML]

`DecisionTreeRegressor` w przeciwieństwie do `DecisionTreeClassifier` przewiduje:

A) wyłącznie etykietę klasy większościowej  
B) prawdopodobieństwo klasy 1 z sigmoidy  
C) liczbę sąsiadów k  
D) wartość ciągłą (typowo średnią targetu w liściu)

---

## Pytanie 3 [łatwe] [ML]

Zrównoważone klasy w Iris (po 50 próbek na klasę) sprawiają, że:

A) accuracy jest łatwiejsza do interpretacji niż przy silnym niezbalansowaniu  
B) drzewo musi używać `criterion='entropy'`  
C) `max_depth` przestaje mieć znaczenie  
D) model automatycznie odzyskuje 4 cechy przy wizualizacji 2D

---

## Pytanie 4 [średnie] [ML]

Podziały „axis-aligned” w drzewie decyzyjnym oznaczają, że:

A) granice są zawsze okręgami wokół centroidów  
B) model dzieli przestrzeń wyłącznie po przekątnej płaszczyzny  
C) progi są prostopadłe do osi cech (regiony prostokątne / schodkowe)  
D) każdy podział musi używać jednocześnie wszystkich cech

---

## Pytanie 5 [średnie] [ML]

Bardzo duża wartość `max_depth` (np. 15) na małym zbiorze treningowym zwykle:

A) gwarantuje najlepszą generalizację na niewidzianych danych  
B) pozwala niemal idealnie dopasować train i niesie silne ryzyko overfittingu  
C) wymusza `criterion='entropy'`  
D) powoduje, że `score()` przestaje zwracać accuracy

---

## Pytanie 6 [średnie] [ML]

`plot_decision_regions` i `export_graphviz` odpowiadają na różne pytania, bo:

A) pierwszy pokazuje podział płaszczyzny cech, drugi – strukturę węzłów i progów drzewa  
B) oba zawsze zwracają tę samą macierz confusion matrix  
C) pierwszy liczy Gini, drugi liczy entropię  
D) oba służą wyłącznie do skalowania cech

---

## Pytanie 7 [średnie] [ML]

Przy samej parze cech działka (`sepal_*`) w Iris typowo:

A) wszystkie trzy klasy są idealnie liniowo rozdzielone  
B) `setosa` oddziela się wyraźniej, a `versicolor` i `virginica` mocno się nakładają  
C) redukcja do 2 cech zwiększa liczbę klas z 3 do 4  
D) płytkie drzewo musi osiągnąć 100% accuracy na train

---

## Pytanie 8 [trudne] [ML]

Funkcja pomocnicza, która dla zadanego `max_depth` trenuje model, rysuje granice i eksportuje graf, jest przydatna, bo:

A) zastępuje całkowicie potrzebę metryk klasyfikacji  
B) gwarantuje brak overfittingu bez zbioru testowego  
C) ułatwia systematyczne porównanie wpływu głębokości na złożoność i dopasowanie  
D) automatycznie dodaje brakujące cechy Iris przy `predict()`

---

## Pytanie 9 [trudne] [ML]

Po wygenerowaniu opisu DOT przez `export_graphviz` do uzyskania obrazu PNG potrzeba:

A) wyłącznie wywołania `classifier.score()`  
B) narzędzia renderującego DOT (np. pydotplus) budującego graf z bufora tekstowego  
C) zamiany `DecisionTreeClassifier` na `LinearRegression`  
D) ustawienia `n_neighbors` w drzewie

---

## Pytanie 10 [trudne] [ML]

W porównaniu z KNN drzewo decyzyjne:

A) buduje jawną strukturę podziałów już podczas `fit()`; predykcja to ścieżka od korzenia do liścia  
B) zapamiętuje tylko dane w `fit()` i odkłada całą decyzję wyłącznie na `predict()` (lazy learning)  
C) zawsze daje gładką liniową granicę jak regresja logistyczna  
D) nie ma parametru `max_depth`

---

## Pytanie 11 [łatwe] [ML]

Wskaźnik Gini równy 0 w węźle oznacza, że:

A) klasy są wymieszane po równo  
B) entropia Shannona wynosi dokładnie 1 bit  
C) model jest gorszy niż przewidywanie średnią  
D) węzeł jest czysty (jedna klasa)

---

## Pytanie 12 [łatwe] [ML]

Użycie `drop_first=True` w one-hot encoding ma na celu głównie:

A) zwiększenie liczby kolumn dummy o jedną  
B) zamianę targetu na wartości ciągłe  
C) uniknięcie pułapki zmiennych fikcyjnych i redukcję kolinearności  
D) wymuszenie `criterion='gini'`

---

## Pytanie 13 [średnie] [ML]

Dla klasyfikacji binarnej przy rozkładzie klas 50/50 entropia Shannona (log₂) osiąga maksimum równe:

A) 1 bit  
B) 0 bitów  
C) 0,5 bita  
D) 2/3 bita

---

## Pytanie 14 [średnie] [ML]

W klasyfikatorze sklearn metoda `predict_proba` zwraca:

A) wyłącznie etykiety klas (0/1)  
B) prawdopodobieństwa przynależności do poszczególnych klas  
C) współczynnik R² na zbiorze testowym  
D) macierz korelacji cech

---

## Pytanie 15 [średnie] [ML]

RMSE w stosunku do MSE to:

A) kwadrat MSE  
B) wartość bezwzględna MSE  
C) accuracy pomniejszona o MSE  
D) pierwiastek kwadratowy z MSE

---

## Pytanie 16 [trudne] [ML]

Dopasowanie `StandardScaler` (`fit`) na całym zbiorze przed podziałem train/test jest ryzykowne, bo:

A) skalowanie zawsze niszczy informacje o klasach  
B) `transform` przestaje działać na danych numerycznych  
C) informacje ze zbioru testowego mogą „przeciec” do parametrów skalera (data leakage)  
D) wymusza użycie drzewa zamiast regresji logistycznej

---

## Pytanie 17 [trudne] [ML]

Przy budowie drzewa algorytm wybiera podział z:

A) największym zyskiem informacyjnym (największy spadek ważonej nieczystości)  
B) najmniejszym zyskiem informacyjnym  
C) najwyższym `max_depth` spośród liści  
D) najniższą accuracy na zbiorze testowym

---

## Pytanie 18 [średnie] [ML]

W KNN mała wartość `n_neighbors` zwykle prowadzi do:

A) maksymalnie wygładzonych granic i silnego underfittingu  
B) bardziej poszaranych granic i większego ryzyka overfittingu  
C) zbudowania drzewa z podziałami prostopadłymi do osi  
D) minimalizacji binary cross-entropy metodą gradientu

---

## Pytanie 19 [łatwe] [ML]

Wartość R² równa 1,0 oznacza:

A) model gorszy niż przewidywanie średnią targetu  
B) model nie lepszy niż średnia (R² = 0)  
C) idealne dopasowanie przewidywań do obserwacji  
D) konieczność użycia `bfill` na brakach

---

## Pytanie 20 [trudne] [ML]

Wysoki stopień `PolynomialFeatures` przy małej liczbie obserwacji:

A) zwiększa ryzyko przeuczenia (model zbyt elastyczny względem danych)  
B) zawsze poprawia generalizację na zbiorze testowym  
C) jest równoważny ustawieniu `max_depth=1` w drzewie  
D) usuwa potrzebę one-hot encodingu zmiennych kategorycznych
