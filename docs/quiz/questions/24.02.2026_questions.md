# Quiz ML Bootcamp – 24.02.2026

---

## Pytanie 1 [łatwe]

Który wzór opisuje aktualizację wag w gradient descent?

A) weights = weights + eta * gradient  
B) weights = weights - eta * gradient  
C) weights = eta * gradient  
D) weights = weights / gradient

---

## Pytanie 2 [średnie]

Po co w train_test_split podaje się random_state?

A) Aby zwiększyć rozmiar zbioru testowego.  
B) Aby zachować proporcje klas.  
C) Aby usunąć duplikaty.  
D) Aby uzyskać powtarzalność podziału przy każdym uruchomieniu.

---

## Pytanie 3 [trudne]

W backward elimination pętla kończy się (break), gdy:

A) Wszystkie pozostałe zmienne mają p-value ≤ poziomu istotności.  
B) Osiągnięto maksymalną liczbę iteracji.  
C) Nie ma już kolumn do usunięcia.  
D) R² spadło poniżej progu.

---

## Pytanie 4 [łatwe]

Równanie modelu LinearRegression to:

A) y = coef_ * x  
B) y = eta * gradient  
C) y = mean(y)  
D) y = intercept_ + coef_[0]*x1 + coef_[1]*x2 + …

---

## Pytanie 5 [średnie]

Dlaczego w get_dummies() stosuje się drop_first=True?

A) Aby przyspieszyć obliczenia.  
B) Aby uniknąć pułapki zmiennych fikcyjnych i redukować kolinearność.  
C) Aby usunąć braki.  
D) Aby zachować pierwszą kategorię jako referencyjną w nazewnictwie.

---

## Pytanie 6 [trudne]

Co zwraca metoda corr() w pandas dla DataFrame?

A) Macierz współczynników korelacji (np. Pearsona) między kolumnami.  
B) Listę par skorelowanych kolumn.  
C) Jedną liczbę – współczynnik dla całego zbioru.  
D) Słownik nazwa_kolumny → wartość korelacji z targetem.

---

## Pytanie 7 [średnie]

Gdy score na zbiorze treningowym jest dużo wyższy niż na testowym, mówimy o:

A) Underfittingu.  
B) Idealnym dopasowaniu.  
C) Błędzie w podziale train/test.  
D) Przeuczeniu (overfitting).

---

## Pytanie 8 [łatwe]

test_size=0.2 w train_test_split oznacza:

A) 20% danych do treningu, 80% do testu.  
B) 2% danych do testu.  
C) 80% danych do treningu, 20% do testu.  
D) 20 próbek do testu.

---

## Pytanie 9 [średnie]

Przed przekazaniem danych do sm.OLS() po get_dummies() wykonuje się .values.astype(float). Dlaczego?

A) Aby przyspieszyć obliczenia.  
B) statsmodels wymaga numpy array float; wartości boolean z get_dummies muszą być 1.0/0.0.  
C) Aby zachować proporcje kategorii.  
D) Aby usunąć braki.

---

## Pytanie 10 [trudne]

Do wizualizacji struktury drzewa w scikit-learn używa się:

A) plot_tree z sklearn.tree (filled=True, rounded=True, feature_names).  
B) export_graphviz.  
C) TreeVisualizer.  
D) draw_tree.

---

## Pytanie 11 [łatwe]

Parametr max_depth w DecisionTreeRegressor służy do:

A) Ustawienia minimalnej liczby próbek w liściu.  
B) Ustawienia liczby cech.  
C) Wyboru metryki podziału.  
D) Ograniczenia głębokości drzewa (liczby poziomów podziałów).

---

## Pytanie 12 [średnie]

W regresji wielomianowej przy wysokim stopniu wielomianu i małej liczbie obserwacji zaleca się:

A) Zwiększyć stopień.  
B) Użyć tylko jednej cechy.  
C) Rozważyć regularyzację (Ridge, Lasso) lub niższy stopień.  
D) Pominąć stałą (include_bias=False).

---

## Pytanie 13 [trudne]

W OLS (statsmodels) zmienna uznawana jest za nieistotną statystycznie, gdy:

A) Jej R² jest niskie.  
B) p-value ≥ 0.05 (przy poziomie istotności 0.05).  
C) Jej współczynnik jest ujemny.  
D) Występuje kolinearność z const.

---

## Pytanie 14 [średnie]

Funkcja np.argmax(ols.pvalues.astype('float')) w backward elimination zwraca:

A) Indeks zmiennej (kolumny) z najwyższym p-value.  
B) Maksymalną wartość p-value.  
C) Liczbę zmiennych do usunięcia.  
D) Listę indeksów wszystkich nieistotnych zmiennych.

---

## Pytanie 15 [łatwe]

Krzywa predykcji DecisionTreeRegressor na jednej cesze ma postać:

A) Prostej.  
B) Krzywej gładkiej (wielomian).  
C) Sinusoidy.  
D) Schodków (stała wartość w każdym przedziale).

---

## Pytanie 16 [trudne]

Usunięcie kolumny z macierzy numpy w backward elimination wykonuje się przez:

A) X[:, max_idx] = None  
B) X.drop(columns=[max_idx])  
C) np.delete(X, max_idx, axis=1)  
D) X = X[:, np.arange(X.shape[1]) != max_idx]

---

## Pytanie 17 [średnie]

PolynomialFeatures(degree=k) w połączeniu z LinearRegression służy do:

A) Regularyzacji L2.  
B) Regresji wielomianowej – rozszerzenia cech (1, X, X², …) i dopasowania liniowego.  
C) Klasyfikacji wieloklasowej.  
D) Redukcji wymiarowości.

---

## Pytanie 18 [łatwe]

Zapis dopasowanego modelu OLS do pliku w statsmodels to:

A) ols.save('model.pickle')  
B) sm.save(ols, 'model.pickle')  
C) ols.to_file('model.pickle')  
D) pickle.dump(ols, file)

---

## Pytanie 19 [trudne]

W regresji drzewa decyzyjnego w każdym liściu przewidywana wartość to zazwyczaj:

A) Mediana targetu w tym węźle.  
B) Wartość ostatniej obserwacji.  
C) Zero.  
D) Średnia (wartość średnia) targetu dla obserwacji w tym węźle.

---

## Pytanie 20 [średnie]

Dlaczego dla jednej cechy X przed przekazaniem do LinearRegression lub DecisionTreeRegressor stosuje się X.reshape(-1, 1)?

A) Aby usunąć braki.  
B) Aby posortować dane.  
C) scikit-learn oczekuje macierzy 2D (próbki × cechy); jedna kolumna to (n, 1).  
D) Aby zwiększyć wydajność.

---
