# Quiz ML Bootcamp – 06.07.2026

---

## Pytanie 1 [łatwe] [ML]

Gdy prawdziwa etykieta y_i = 0, strata binary cross-entropy dla jednej obserwacji wynosi:

A) −log(y_pred_i)  
B) −log(1 − y_pred_i)  
C) (y_i − y_pred_i)²  
D) |y_i − y_pred_i|

---

## Pytanie 2 [łatwe] [ML]

W ręcznej implementacji gradient descent poprawna aktualizacja wag ma postać:

A) weights = weights − eta · gradient  
B) weights = weights + eta · gradient  
C) weights = eta − weights · gradient  
D) weights = gradient / eta

---

## Pytanie 3 [łatwe] [Python]

Większość algorytmów uczenia maszynowego w scikit-learn wobec wartości NaN:

A) ignoruje je bez wpływu na wynik  
B) automatycznie je imputuje średnią  
C) wymaga wcześniejszej obsługi braków (np. imputacji)  
D) konwertuje je na zero bez ostrzeżenia

---

## Pytanie 4 [średnie] [ML]

Raport `classification_report` dla klasyfikacji binarnej zawiera m.in.:

A) wyłącznie accuracy globalną  
B) współczynnik R² i RMSE  
C) precision, recall, F1-score i support per klasa  
D) wyłącznie macierz kowariancji cech

---

## Pytanie 5 [średnie] [ML]

Typowy objaw **overfittingu** (przeuczenia) to sytuacja, gdy:

A) score na train i test są podobne i niskie  
B) score na train jest wyraźnie wyższy niż na test  
C) score na test jest wyraźnie wyższy niż na train  
D) oba score są identyczne i bliskie 1,0

---

## Pytanie 6 [łatwe] [ML]

Parametr `stratify=y` w `train_test_split` służy do:

A) zachowania proporcji klas w zbiorach train i test  
B) automatycznego skalowania cech StandardScalerem  
C) ustawienia udziału zbioru testowego na 50%  
D) imputacji brakujących wartości w targetcie

---

## Pytanie 7 [trudne] [ML]

Gdy y_i = 0, a przewidywane prawdopodobieństwo y_pred_i → 1, strata −log(1 − y_pred_i):

A) maleje do zera  
B) pozostaje stała i niska  
C) przyjmuje wartości ujemne  
D) rośnie (w granicy do +∞)

---

## Pytanie 8 [średnie] [ML]

Równanie normalne (Normal Equation) dla regresji liniowej ma postać:

A) θ = (XᵀX)⁻¹ Xᵀy  
B) θ = X · y  
C) θ = XᵀX · y  
D) θ = (XXᵀ)⁻¹y

---

## Pytanie 9 [trudne] [ML]

W standardowej macierzy pomyłek (confusion matrix) z scikit-learn:

A) wiersze to predykcje modelu, kolumny to prawdziwe etykiety  
B) elementy poza diagonalą zawsze oznaczają poprawne trafienia  
C) wiersze i kolumny reprezentują kolejne cechy wejściowe  
D) wiersze to prawdziwe etykiety, kolumny to predykcje modelu

---

## Pytanie 10 [łatwe] [ML]

Metoda `score()` estymatora `LinearRegression` zwraca:

A) średni błąd bezwzględny (MAE)  
B) accuracy klasyfikacji  
C) współczynnik determinacji R²  
D) pierwiastek z błędu średniokwadratowego (RMSE)

---

## Pytanie 11 [średnie] [ML]

Regresja wielomianowa w tym bootcampie realizowana jest jako:

A) osobna klasa PolynomialRegression w sklearn  
B) PolynomialFeatures (rozszerzenie cech) + LinearRegression  
C) wyłącznie logarytmiczna transformacja targetu  
D) DecisionTreeRegressor z parametrem degree

---

## Pytanie 12 [średnie] [Python]

W szeregu czasowym metoda `fillna(method='bfill')` uzupełnia braki:

A) wartościami z wcześniejszych obserwacji (forward fill)  
B) zerami  
C) wartościami z późniejszych obserwacji  
D) średnią arytmetyczną całego szeregu

---

## Pytanie 13 [trudne] [ML]

Współczynnik R² ujemny dla modelu regresji oznacza, że model:

A) jest gorszy niż przewidywanie średnią wartością targetu  
B) idealnie dopasowuje dane treningowe  
C) ma wyłącznie dodatnie współczynniki regresji  
D) zawsze wskazuje na underfitting

---

## Pytanie 14 [trudne] [ML]

W automatycznej backward elimination (poziom istotności sl = 0,05) zmienną usuwa się, gdy jej p-value:

A) jest mniejsze niż 0,05  
B) jest większe niż 0,05  
C) jest dokładnie równe 0,05  
D) jest równe zeru

---

## Pytanie 15 [średnie] [ML]

W `DecisionTreeRegressor` wartość przewidywana w liściu to zwykle:

A) mediana całego zbioru treningowego  
B) średnia wartości targetu w regionie (liściu) drzewa  
C) wynik funkcji sigmoidalnej  
D) ostatnia obserwowana wartość w zbiorze

---

## Pytanie 16 [łatwe] [ML]

Parametr `random_state` w `train_test_split` służy do:

A) automatycznej stratifikacji klas  
B) zwiększenia udziału zbioru testowego  
C) powtarzalności podziału (ten sam podział przy tym samym ziarnie)  
D) wyboru jednej cechy do modelu

---

## Pytanie 17 [trudne] [ML]

Metryka accuracy przy **silnie niezbalansowanych** klasach:

A) zawsze w pełni odzwierciedla jakość modelu  
B) może być myląco wysoka mimo słabego wykrywania mniejszościowej klasy  
C) jest tożsama ze średnią F1-score  
D) zastępuje konieczność analizy macierzy pomyłek

---

## Pytanie 18 [średnie] [ML]

RMSE w `sklearn.metrics` oblicza się jako:

A) `mean_squared_error(y_true, y_pred, squared=False)`  
B) `mean_absolute_error(y_true, y_pred)`  
C) `r2_score(y_true, y_pred)`  
D) `accuracy_score(y_true, y_pred)`

---

## Pytanie 19 [trudne] [ML]

Dla argumentu x = 0 funkcja sigmoidalna σ(x) = 1 / (1 + e^(−x)) przyjmuje wartość:

A) 0  
B) 1  
C) 0,5  
D) −1

---

## Pytanie 20 [średnie] [Python]

Parametr `drop_first=True` w `pd.get_dummies()` stosuje się, aby:

A) zwiększyć liczbę kolumn o jedną na każdą kategorię  
B) usunąć wiersze zawierające braki danych  
C) zakodować zmienną docelową na wartości 0/1  
D) uniknąć pułapki zmiennych fikcyjnych i nadmiarowej kolinearności
