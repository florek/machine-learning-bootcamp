# Quiz ML Bootcamp – 19.05.2026

---

## Pytanie 1 [łatwe] [ML]

Gdy prawdziwa etykieta y_i = 1, strata binary cross-entropy dla jednej obserwacji wynosi:

A) −log(1 − y_pred_i)  
B) (y_i − y_pred_i)²  
C) −log(y_pred_i)  
D) |y_i − y_pred_i|

---

## Pytanie 2 [średnie] [ML]

W pipeline klasyfikacji ze StandardScaler poprawna kolejność to:

A) fit na train+test, potem transform na train  
B) fit na train, transform na train i test tymi samymi parametrami  
C) fit i transform osobno na test  
D) fit tylko na y_train

---

## Pytanie 3 [łatwe] [ML]

Dla bardzo dużego dodatniego scoringu (x → +∞) wartość sigmoidy σ(x) jest:

A) bliska 0,5  
B) bliska 0  
C) ujemna  
D) bliska 1

---

## Pytanie 4 [trudne] [ML]

W macierzy pomyłek (confusion matrix) dla klasyfikacji binarnej elementy na **diagonali** oznaczają:

A) poprawne klasyfikacje (zgodność prawdy z predykcją)  
B) wyłącznie fałszywe pozytywy  
C) prawdopodobieństwa klas  
D) liczbę cech wejściowych

---

## Pytanie 5 [średnie] [ML]

Metoda `predict_proba` w LogisticRegression zwraca:

A) wyłącznie etykiety 0 lub 1  
B) prawdopodobieństwa przynależności do poszczególnych klas  
C) współczynnik R²  
D) znormalizowane cechy wejściowe

---

## Pytanie 6 [łatwe] [ML]

LogisticRegression w scikit-learn służy przede wszystkim do:

A) prognozowania ciągłej zmiennej docelowej (regresji)  
B) klasteryzacji bez etykiet  
C) klasyfikacji (przypisywania klas)  
D) imputacji brakujących wartości

---

## Pytanie 7 [średnie] [ML]

Funkcja kosztu w regresji logistycznej (minimalizowana w treningu) to:

A) średnia ze strat binary cross-entropy po wszystkich obserwacjach  
B) suma kwadratów różnic (y − y_pred)²  
C) średni błąd bezwzględny (MAE)  
D) wyłącznie −log(y_pred) bez uśredniania

---

## Pytanie 8 [trudne] [ML]

Gdy y = 1, a przewidywane prawdopodobieństwo y_pred → 0, strata −log(y_pred):

A) maleje do zera  
B) pozostaje stała  
C) przyjmuje wartości ujemne  
D) rośnie (w granicy do +∞)

---

## Pytanie 9 [średnie] [Python]

Parametr `drop_first=True` w `pd.get_dummies()` stosuje się, aby:

A) zwiększyć liczbę kolumn o jedną na kategorię  
B) usunąć wiersze z brakami  
C) uniknąć pułapki zmiennych fikcyjnych i nadmiarowej kolinearności  
D) zakodować zmienną docelową

---

## Pytanie 10 [łatwe] [ML]

Domyślny próg decyzyjny w klasyfikacji binarnej (predict po regresji logistycznej) to zwykle:

A) 0  
B) 0,5  
C) 1  
D) mediana y_pred na zbiorze treningowym

---

## Pytanie 11 [średnie] [ML]

Krzywa predykcji DecisionTreeRegressor przy nieliniowej zależności ma charakterystyczny kształt:

A) schodków (stałe wartości na przedziałach)  
B) prostej liniowej  
C) sigmoidy  
D) wykładniczej

---

## Pytanie 12 [trudne] [ML]

W automatycznej backward elimination indeks zmiennej do usunięcia wyznacza się przez:

A) np.argmin(ols.pvalues)  
B) najniższy współczynnik R²  
C) najwyższą korelację z targetem  
D) np.argmax(ols.pvalues.astype(float))

---

## Pytanie 13 [łatwe] [ML]

Parametr `random_state` w `train_test_split` służy do:

A) automatycznej stratifikacji klas  
B) zwiększenia udziału zbioru testowego  
C) powtarzalności podziału (ten sam podział przy tym samym ziarnie)  
D) wyboru jednej cechy do modelu

---

## Pytanie 14 [średnie] [ML]

RMSE w sklearn.metrics oblicza się jako:

A) mean_absolute_error(y_true, y_pred)  
B) mean_squared_error(y_true, y_pred, squared=False)  
C) r2_score(y_true, y_pred)  
D) accuracy_score(y_true, y_pred)

---

## Pytanie 15 [trudne] [ML]

Po `scaler.fit(X_train)` poprawne skalowanie zbioru testowego to:

A) scaler.transform(X_test)  
B) scaler.fit_transform(X_test) z osobnym fit na test  
C) brak skalowania testu  
D) scaler.fit(X_test) przed transform

---

## Pytanie 16 [trudne] [ML]

Współczynnik R² = 0 dla modelu regresji oznacza, że model:

A) idealnie dopasowuje dane  
B) jest zawsze lepszy niż przewidywanie średnią  
C) ma wyłącznie dodatnie współczynniki  
D) nie jest lepszy niż przewidywanie średnią wartością targetu

---

## Pytanie 17 [łatwe] [ML]

MAE (Mean Absolute Error) to średnia z:

A) kwadratów błędów  
B) błędów bez wartości bezwzględnej  
C) wartości bezwzględnych błędów |y_true − y_pred|  
D) maksymalnego pojedynczego błędu

---

## Pytanie 18 [średnie] [ML]

Regresja wielomianowa w tym bootcampie realizowana jest jako:

A) osobna klasa PolynomialRegression w sklearn  
B) PolynomialFeatures (rozszerzenie cech) + LinearRegression  
C) wyłącznie logarytmiczna transformacja targetu  
D) DecisionTreeRegressor z parametrem degree

---

## Pytanie 19 [trudne] [ML]

Metryka `accuracy_score(y_test, y_pred)` porównuje:

A) rzeczywiste etykiety z przewidywanymi etykietami klas  
B) rzeczywiste etykiety z wektorami predict_proba  
C) wyłącznie intercept modelu z zerem  
D) średnią kwadratową błędów regresji

---

## Pytanie 20 [łatwe] [ML]

Zbiór `load_breast_cancer()` z scikit-learn zwraca strukturę zawierającą m.in.:

A) wagi sieci neuronowej i historię epok  
B) wyłącznie ścieżkę do pliku CSV na dysku  
C) listę hiperparametrów GridSearchCV  
D) macierz cech `data` oraz wektor etykiet `target` (klasy 0/1)
