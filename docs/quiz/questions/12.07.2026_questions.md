# Quiz ML Bootcamp – 12.07.2026

---

## Pytanie 1 [łatwe] [ML]

Dla klasyfikacji binarnej z rozkładem klas 50/50 wskaźnik Gini osiąga maksimum równe:

A) 1,0  
B) 0,5  
C) 0,667  
D) 0,0

---

## Pytanie 2 [łatwe] [ML]

Domyślne kryterium podziału węzła w `DecisionTreeClassifier` (scikit-learn) to:

A) `criterion='entropy'`  
B) `criterion='mse'`  
C) `criterion='gini'`  
D) `criterion='manhattan'`

---

## Pytanie 3 [łatwe] [Python]

Parametr `drop_first=True` w `pd.get_dummies()` służy przede wszystkim do:

A) automatycznego usuwania wierszy z brakami danych  
B) wymuszenia kodowania tylko zmiennych numerycznych  
C) zachowania wszystkich kategorii bez redukcji wymiaru  
D) uniknięcia pułapki zmiennych fikcyjnych i redukcji kolinearności

---

## Pytanie 4 [łatwe] [ML]

Algorytm K-Nearest Neighbors jest klasyfikowany jako „leniwe uczenie” (lazy learning), ponieważ podczas `fit()`:

A) głównie zapamiętuje dane treningowe bez budowy jawnej struktury modelu  
B) minimalizuje funkcję kosztu binary cross-entropy  
C) rekurencyjnie buduje drzewo decyzyjne z podziałami prostopadłymi do osi  
D) uczy współczynniki regresji liniowej metodą najmniejszych kwadratów

---

## Pytanie 5 [łatwe] [ML]

Metoda `score()` estymatora `LinearRegression` zwraca:

A) dokładność klasyfikacji (accuracy)  
B) współczynnik determinacji R²  
C) średni błąd bezwzględny (MAE)  
D) pierwiastek z błędu średniokwadratowego (RMSE)

---

## Pytanie 6 [łatwe] [Python]

Po imputacji kolumny kategorycznej metodą `SimpleImputer` często stosuje się `.ravel()`, ponieważ:

A) imputer zwraca tablicę 2D, a kolumna Pandas musi być 1D  
B) `.ravel()` automatycznie koduje kategorie metodą one-hot  
C) `.ravel()` zamienia wartości NaN na średnią kolumny  
D) `.ravel()` wymusza typ float wymagany przez statsmodels

---

## Pytanie 7 [średnie] [ML]

W formule zysku informacyjnego (IG) drugi składnik odejmowany od entropii (lub Gini) węzła rodzica to:

A) entropia rodzica pomnożona przez liczbę klas  
B) suma entropii wszystkich cech w zbiorze danych  
C) entropia wyłącznie największego węzła potomnego  
D) ważona średnia entropii (lub Gini) węzłów potomnych

---

## Pytanie 8 [średnie] [ML]

Regresja wielomianowa w scikit-learn polega na:

A) rozszerzeniu cech o potęgi (np. X, X², X³) i dopasowaniu zwykłej regresji liniowej do rozszerzonej macierzy  
B) zastąpieniu regresji liniowej algorytmem KNN z dużym k  
C) minimalizacji entropii krzyżowej zamiast błędu kwadratowego  
D) budowie drzewa decyzyjnego bez ograniczenia głębokości

---

## Pytanie 9 [średnie] [ML]

Metoda `predict_proba()` w `LogisticRegression` zwraca:

A) wyłącznie etykiety klas 0 lub 1 po progu 0,5  
B) prawdopodobieństwa przynależności do każdej klasy  
C) współczynniki regresji dla poszczególnych cech  
D) odległości euklidesowe do k najbliższych sąsiadów

---

## Pytanie 10 [średnie] [ML]

W kontekście KNN pojęcie „curse of dimensionality” (klątwa wymiarowości) oznacza, że:

A) przy dużej liczbie cech odległości euklidesowe stają się mniej rozróżniające, co pogarsza sensowność sąsiedztwa  
B) KNN zawsze wymaga dokładnie 4 cech jak w zbiorze Iris  
C) im więcej wymiarów, tym mniejsze ryzyko overfittingu przy małym k  
D) skalowanie cech StandardScalerem jest zbędne przy wielu wymiarach

---

## Pytanie 11 [średnie] [ML]

W automatycznej backward elimination (statsmodels) w każdej iteracji usuwa się zmienną o:

A) najniższym współczynniku regresji  
B) najwyższym p-value (najmniej istotną statystycznie)  
C) najwyższej korelacji z targetem  
D) najmniejszej liczbie obserwacji niezerowych

---

## Pytanie 12 [średnie] [Python]

Metoda `bfill` (backward fill) przy uzupełnianiu braków w szeregu czasowym używanym do prognozowania:

A) może wprowadzić data leakage, bo korzysta z przyszłych obserwacji  
B) jest zawsze bezpieczniejsza niż forward fill  
C) uzupełnia braki wyłącznie wartościami z wcześniejszych obserwacji  
D) działa identycznie jak interpolacja liniowa między sąsiadami

---

## Pytanie 13 [średnie] [ML]

W regresji drzewa decyzyjnego (`DecisionTreeRegressor`) wartość przewidywana w liściu to typowo:

A) współczynnik regresji liniowej dopasowany lokalnie  
B) mediana wszystkich obserwacji w całym zbiorze treningowym  
C) prawdopodobieństwo klasy 1 z funkcji sigmoidalnej  
D) średnia wartości targetu w regionie (przedziale) przypisanym do tego liścia

---

## Pytanie 14 [średnie] [ML]

Entropia Shannona (log₂) dla rozkładu binarnego [0,95, 0,05] w porównaniu z [0,5, 0,5] jest:

A) wyższa, bo dominuje jedna klasa  
B) identyczna dla każdego rozkładu binarnego  
C) znacznie niższa – węzeł jest prawie czysty (mała niepewność)  
D) równa dokładnie 1 bit w obu przypadkach

---

## Pytanie 15 [trudne] [ML]

Dla klasyfikacji binarnej przy rozkładzie 50/50 maksymalne wartości wskaźnika Gini i entropii Shannona (log₂) wynoszą odpowiednio:

A) Gini = 0,5; entropia = 1 bit  
B) Gini = 1,0; entropia = 0,5 bit  
C) Gini = 0,667; entropia = 1,585 bit  
D) Gini = 0,0; entropia = 1 bit

---

## Pytanie 16 [trudne] [ML]

Ujemny współczynnik R² (r2_score) dla modelu regresji oznacza, że model:

A) idealnie dopasowuje dane treningowe  
B) ma zbyt mało cech i wymaga PolynomialFeatures  
C) został wytrenowany wyłącznie na zbiorze testowym  
D) jest gorszy niż przewidywanie średnią wartością targetu

---

## Pytanie 17 [trudne] [ML]

Parametr `min_samples_leaf` w drzewie decyzyjnym (klasyfikacja lub regresja) służy do:

A) wymuszenia podziału train/test w proporcji 80/20  
B) ustalenia minimalnej liczby próbek w liściu – ogranicza zbyt drobne podziały i przeuczenie  
C) wyboru liczby najbliższych sąsiadów w algorytmie KNN  
D) automatycznego kodowania zmiennych kategorycznych metodą one-hot

---

## Pytanie 18 [trudne] [ML]

W pipeline klasyfikacji z `StandardScaler` poprawna kolejność operacji to:

A) `fit` skalera wyłącznie na train, następnie `transform` na train i test (bez `fit` na test)  
B) `fit` skalera na połączonym train i test, aby zachować te same statystyki  
C) `fit` skalera wyłącznie na test, następnie `transform` na train  
D) pominięcie skalowania, bo LogisticRegression nie wymaga standaryzacji

---

## Pytanie 19 [trudne] [ML]

W porównaniu z MAE, metryka MSE (Mean Squared Error):

A) ma te same jednostki co target bez żadnej transformacji  
B) ignoruje błędy większe niż średnia wartość targetu  
C) silniej karze duże błędy dzięki kwadratowi różnicy  
D) zawsze przyjmuje wartości ujemne

---

## Pytanie 20 [trudne] [Python]

Wywołanie `scipy.stats.entropy([0.5, 0.5])` **bez** parametru `base=2` w kontekście drzew decyzyjnych:

A) zwraca wynik w bitach zgodny ze wzorem Shannona z log₂  
B) używa logarytmu naturalnego – wynik ma inną skalę niż entropia w bitach (log₂)  
C) zawsze zwraca dokładnie 1,0  
D) zwraca NaN z powodu braku podstawy logarytmu
