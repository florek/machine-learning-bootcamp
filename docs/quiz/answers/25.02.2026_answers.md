# Klucz odpowiedzi – 25.02.2026

| Pytanie | Poprawna | Krótkie uzasadnienie |
|---------|----------|----------------------|
| 1 | C | score() zwraca R² (współczynnik determinacji). |
| 2 | C | add_constant() dodaje kolumnę jedynek dla wyrazu wolnego (intercept) w OLS. |
| 3 | D | Pipeline: EDA, czyszczenie (duplikaty, braki), feature engineering, train/test split, trenowanie i ocena. |
| 4 | B | make_regression() generuje syntetyczne dane do regresji. |
| 5 | A | Gradient MSE: (2/m) * X.T.dot(X.dot(weights) - Y). |
| 6 | C | Podobne i wysokie score na train i test = dobre dopasowanie bez overfittingu. |
| 7 | B | drop_first=True unika dummy variable trap i redukuje kolinearność. |
| 8 | D | Model trenuje na train, ocenia na test. |
| 9 | C | Lista predictors musi być zsynchronizowana z kolumnami X_train_numpy. |
| 10 | B | MAE jest w tych samych jednostkach co zmienna docelowa. |
| 11 | A | max_depth ogranicza głębokość drzewa (liczbę poziomów). |
| 12 | D | Model liniowy względem parametrów; nieliniowość z transformacji cech. |
| 13 | B | ols.summary() pokazuje p-value, R², współczynniki i inne statystyki. |
| 14 | C | Forward Selection startuje od pustego modelu i dodaje zmienne. |
| 15 | A | Krzywa drzewa to schodki – stała wartość w każdym przedziale. |
| 16 | D | PolynomialFeatures(degree=3) daje 1, X, X², X³ (ze stałą jeśli include_bias). |
| 17 | B | plot_tree z sklearn.tree (filled=True, rounded=True, feature_names). |
| 18 | A | Zmienna istotna gdy p-value < 0.05 (przy poziomie 0.05). |
| 19 | C | W liściu przewiduje się średnią targetu dla obserwacji w węźle. |
| 20 | D | scikit-learn wymaga macierzy 2D (próbki × cechy). |
