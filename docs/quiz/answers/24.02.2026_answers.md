# Klucz odpowiedzi – 24.02.2026

| Pytanie | Poprawna | Krótkie uzasadnienie |
|---------|----------|----------------------|
| 1 | B | W gradient descent wagi aktualizuje się: weights = weights - eta * gradient. |
| 2 | D | random_state zapewnia powtarzalność podziału (te same train/test przy każdym uruchomieniu). |
| 3 | A | Pętla kończy się, gdy wszystkie zmienne mają p-value ≤ poziomu istotności (np. 0.05). |
| 4 | C | Równanie: y = intercept_ + coef_[0]*x1 + coef_[1]*x2 + … |
| 5 | B | drop_first=True unika dummy variable trap i redukuje kolinearność. |
| 6 | A | corr() zwraca macierz współczynników korelacji (np. Pearsona) między kolumnami. |
| 7 | D | Duża przewaga score na train nad test to przeuczenie (overfitting). |
| 8 | C | test_size=0.2 oznacza 20% danych do zbioru testowego, 80% do treningu. |
| 9 | B | statsmodels wymaga numpy array float; boolean z get_dummies() muszą być 1.0/0.0. |
| 10 | A | plot_tree z sklearn.tree (filled=True, rounded=True, feature_names) służy do wizualizacji drzewa. |
| 11 | D | max_depth ogranicza głębokość drzewa (liczbę poziomów podziałów). |
| 12 | C | Przy wysokim stopniu i małej liczbie danych: regularyzacja lub niższy stopień. |
| 13 | B | W OLS zmienna nieistotna statystycznie gdy p-value ≥ 0.05. |
| 14 | A | np.argmax() zwraca indeks elementu z najwyższą wartością (tu: indeks zmiennej z max p-value). |
| 15 | D | Krzywa predykcji drzewa to „schodki” – stała wartość w każdym przedziale. |
| 16 | C | np.delete(X, max_idx, axis=1) usuwa kolumnę o indeksie max_idx. |
| 17 | B | PolynomialFeatures + LinearRegression = regresja wielomianowa (rozszerzenie cech). |
| 18 | A | W statsmodels zapis modelu: ols.save('model.pickle'). |
| 19 | D | W liściu drzewa przewiduje się zazwyczaj średnią targetu dla obserwacji w tym węźle. |
| 20 | C | scikit-learn oczekuje macierzy 2D (próbki × cechy); reshape(-1, 1) daje jedną kolumnę. |
