# Klucz odpowiedzi – 23.09.2026

Skład: pytania 1–10 = ostatnia lekcja (DecisionTreeClassifier); 11–20 = wcześniejsze lekcje.

| Pytanie | Poprawna | Krótkie uzasadnienie |
|---------|----------|----------------------|
| 1 | C | Dla klasyfikatora `score()` zwraca accuracy – odsetek poprawnie przewidzianych etykiet. |
| 2 | A | `max_depth=1` ogranicza drzewo do jednego poziomu podziału (jeden próg na wybranej cesze). |
| 3 | D | W klasyfikacji liść przewiduje klasę większościową wśród obserwacji w danym regionie. |
| 4 | B | `plot_decision_regions` rysuje kolorowe regiony klas oraz punkty w przestrzeni cech. |
| 5 | A | `export_graphviz` generuje opis DOT struktury drzewa (progi, węzły, nazwy cech i klas) do renderowania grafu. |
| 6 | C | Drzewo dzieli przestrzeń podziałami prostopadłymi do osi – regiony są prostokątne / schodkowe. |
| 7 | B | Większa głębokość daje bardziej złożone regiony i zwykle wyższą accuracy na zbiorze treningowym. |
| 8 | A | Wysoki wynik na train przy głębokim drzewie może oznaczać overfitting – potrzebna ocena na teście. |
| 9 | D | Odrzucenie cech upraszcza wizualizację, ale traci się informację z pozostałych atrybutów. |
| 10 | D | Drzewo: regiony osiowe; KNN: nieregularne zależne od k; regresja logistyczna: granica liniowa. |
| 11 | B | Dla klasyfikacji binarnej Gini ∈ [0, 0,5]; maksimum 0,5 przy rozkładzie 50/50. |
| 12 | A | IG = nieczystość rodzica − ważona średnia nieczystości dzieci; drzewo wybiera największy IG. |
| 13 | C | Skaler dopasowuje się tylko na train; te same parametry stosuje się przez `transform` do train i test. |
| 14 | B | Bez `base=2` scipy stosuje logarytm naturalny – to inna skala niż entropia Shannona w bitach. |
| 15 | D | MSE kwadratuje błędy, więc duże odchylenia są karane mocniej niż przy MAE. |
| 16 | B | Ujemne R² oznacza model gorszy od naiwnego przewidywania średnią targetu. |
| 17 | A | Typowy pipeline: `PolynomialFeatures` rozszerza cechy, potem `LinearRegression` na macierzy rozszerzonej. |
| 18 | C | `bfill` wypełnia braki wartościami z przyszłości – w prognozowaniu grozi to data leakage. |
| 19 | A | KNN to lazy learning: `fit()` zapamiętuje dane, decyzja zapada przy `predict()`. |
| 20 | B | Backward elimination usuwa w iteracji zmienną o najwyższym p-value (przy progu istotności). |
