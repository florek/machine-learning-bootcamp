# Klucz odpowiedzi – 24.09.2026

Skład: pytania 1–10 = ostatnia lekcja (DecisionTreeClassifier); 11–20 = wcześniejsze lekcje.

| Pytanie | Poprawna | Krótkie uzasadnienie |
|---------|----------|----------------------|
| 1 | B | Domyślne kryterium podziału w `DecisionTreeClassifier` to `'gini'` (alternatywa: `'entropy'`). |
| 2 | D | Regresor przewiduje wartość ciągłą – typowo średnią targetu w liściu; klasyfikator przewiduje etykietę klasy. |
| 3 | A | Przy zrównoważonych klasach (po 50) accuracy jest łatwiejsza do interpretacji niż przy silnym niezbalansowaniu. |
| 4 | C | Podziały axis-aligned są prostopadłe do osi cech, więc regiony w 2D są prostokątne / schodkowe. |
| 5 | B | Bardzo głębokie drzewo na małym zbiorze może niemal idealnie dopasować train → silne ryzyko overfittingu. |
| 6 | A | `plot_decision_regions` pokazuje podział przestrzeni cech; `export_graphviz` – strukturę węzłów i progów. |
| 7 | B | Na cechach działka `setosa` oddziela się wyraźniej, a `versicolor` i `virginica` mocno się nakładają. |
| 8 | C | Wspólna funkcja (train + granice + graf) ułatwia systematyczne porównanie różnych wartości `max_depth`. |
| 9 | B | `export_graphviz` daje DOT; do PNG potrzeba renderu (np. pydotplus z bufora tekstowego). |
| 10 | A | Drzewo buduje strukturę podziałów w `fit()`; KNN to lazy learning z decyzją przy `predict()`. |
| 11 | D | Gini = 0 oznacza węzeł czysty – wszystkie obserwacje należą do jednej klasy. |
| 12 | C | `drop_first=True` unika pułapki zmiennych fikcyjnych i redukuje kolinearność. |
| 13 | A | Przy p = q = 0,5 entropia Shannona (log₂) osiąga maksimum 1 bit. |
| 14 | B | `predict_proba` zwraca prawdopodobieństwa per klasa; `predict` – etykiety. |
| 15 | D | RMSE to pierwiastek kwadratowy z MSE (w sklearn często `squared=False`). |
| 16 | C | `fit` skalera na całym zbiorze przed splitem może wprowadzić data leakage ze zbioru testowego. |
| 17 | A | Drzewo wybiera podział z największym zyskiem informacyjnym (największy spadek ważonej nieczystości). |
| 18 | B | Małe k daje poszarpane granice i większe ryzyko overfittingu; duże k wygładza. |
| 19 | C | R² = 1,0 oznacza idealne dopasowanie przewidywań do obserwowanych wartości. |
| 20 | A | Wysoki stopień wielomianu przy małej liczbie próbek zwiększa elastyczność modelu i ryzyko przeuczenia. |
