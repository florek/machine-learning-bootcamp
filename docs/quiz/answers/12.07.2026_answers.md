# Klucz odpowiedzi – 12.07.2026

| Pytanie | Poprawna | Krótkie uzasadnienie |
|---------|----------|----------------------|
| 1 | B | Dla klasyfikacji binarnej Gini ∈ [0, 0,5]; maksimum 0,5 przy rozkładzie 50/50. |
| 2 | C | DecisionTreeClassifier domyślnie używa criterion='gini' do wyboru najlepszego podziału węzła. |
| 3 | D | drop_first=True usuwa jedną kategorię referencyjną, unikając pułapki zmiennych fikcyjnych i kolinearności. |
| 4 | A | KNN podczas fit() zapamiętuje dane; klasyfikacja następuje dopiero przy predict() przez głosowanie sąsiadów. |
| 5 | B | Metoda score() w LinearRegression zwraca współczynnik determinacji R². |
| 6 | A | SimpleImputer zwraca tablicę 2D; ravel() konwertuje wynik do kolumny 1D wymaganej przez Pandas. |
| 7 | D | IG odejmuje od nieczystości rodzica ważoną średnią nieczystości węzłów potomnych (waga = liczność dziecka / rodzica). |
| 8 | A | Regresja wielomianowa rozszerza cechy o potęgi i stosuje LinearRegression – model liniowy w parametrach, nieliniowy względem oryginalnej cechy. |
| 9 | B | predict_proba() zwraca prawdopodobieństwa przynależności do każdej klasy (wynik sigmoidy w klasyfikacji binarnej). |
| 10 | A | Przy wielu wymiarach odległości tracą rozróżnialność – sąsiedztwo staje się mniej sensowne (curse of dimensionality). |
| 11 | B | Backward elimination w każdej iteracji usuwa zmienną o najwyższym p-value (najmniej istotną przy sl=0,05). |
| 12 | A | bfill uzupełnia braki wartościami z przyszłych obserwacji – w prognozowaniu może to wprowadzić data leakage. |
| 13 | D | DecisionTreeRegressor przewiduje w liściu stałą wartość – typowo średnią targetu w przypisanym regionie (schodkowa krzywa). |
| 14 | C | Rozkład 95/5 jest prawie czysty – entropia jest znacznie niższa niż przy maksymalnej niepewności 50/50. |
| 15 | A | Dla dwóch klas przy 50/50: maksimum Gini = 0,5, maksimum entropii (log₂) = 1 bit. |
| 16 | D | R² < 0 oznacza, że model jest gorszy niż przewidywanie średnią wartością targetu. |
| 17 | B | min_samples_leaf wymusza minimalną liczność liścia, ograniczając zbyt drobne podziały i ryzyko przeuczenia. |
| 18 | A | StandardScaler: fit tylko na train, transform na train i test – fit na test powodowałby data leakage. |
| 19 | C | MSE kwadratuje błędy, więc silniej karze duże odchylenia niż MAE (średnia wartości bezwzględnych). |
| 20 | B | Bez base=2 scipy.stats.entropy stosuje logarytm naturalny – inna skala niż entropia Shannona w bitach (log₂). |
