# Klucz odpowiedzi – 07.07.2026

| Pytanie | Poprawna | Krótkie uzasadnienie |
|---------|----------|----------------------|
| 1 | B | Dla y=1 strata binary cross-entropy to −log(y_pred); im bliżej 1 prawdopodobieństwa, tym mniejsza kara. |
| 2 | D | KNeighborsClassifier klasyfikuje obserwację na podstawie głosowania k najbliższych sąsiadów w przestrzeni cech. |
| 3 | C | Forward fill (ffill) uzupełnia braki wartościami z wcześniejszych obserwacji szeregu czasowego. |
| 4 | A | Iris ma trzy klasy docelowe – to przykład klasyfikacji wieloklasowej, nie regresji. |
| 5 | B | random_state zapewnia powtarzalny losowy podział przy tym samym ziarnie generatora. |
| 6 | C | Estymatory sklearn nie akceptują NaN – braki trzeba imputować lub usunąć przed fit(). |
| 7 | D | classification_report zawiera precision, recall, F1-score i support dla każdej klasy. |
| 8 | A | Równanie normalne: θ = (XᵀX)⁻¹ Xᵀy – algebraiczne rozwiązanie regresji liniowej. |
| 9 | C | Recall mierzy, jaki odsetek obserwacji danej klasy model poprawnie wykrył (true positive rate). |
| 10 | B | Overfitting: model świetnie na train, gorzej na test – score(train) wyraźnie wyższy niż score(test). |
| 11 | A | Parametr n_neighbors określa liczbę sąsiadów branych pod uwagę przy głosowaniu w KNN. |
| 12 | D | drop_first=True usuwa kategorię referencyjną i ogranicza kolinearność (dummy variable trap). |
| 13 | C | Liść drzewa regresji przewiduje stałą – zwykle średnią targetu w danym regionie. |
| 14 | B | StandardScaler: fit tylko na train, transform na train i test – unika data leakage. |
| 15 | D | Większe k w KNN daje wygładzone granice decyzyjne i mniejszą wrażliwość na pojedyncze punkty. |
| 16 | A | R² < 0 oznacza, że model jest gorszy niż przewidywanie średnią wartością targetu. |
| 17 | B | Przy y=0 i y_pred→1 strata −log(1−y_pred) rośnie bez ograniczenia – model myli się na rzecz klasy 1. |
| 18 | C | bfill uzupełnia braki z późniejszych obserwacji – w prognozowaniu może wprowadzić data leakage. |
| 19 | A | W sklearn wiersze macierzy pomyłek to prawdziwe etykiety, kolumny to predykcje modelu. |
| 20 | D | KNN zapamiętuje dane przy fit(); klasyfikacja odbywa się dopiero przy predict() – lazy learning. |
